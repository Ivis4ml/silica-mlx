# Manual API Reference

The hand-curated walk through every public class, function, and method
in `silica.*`. Where the auto-generated reference under {doc}`api/index`
is exhaustive and lives in lock-step with the source tree, this page
classifies each symbol as **public / internal / protocol / stub** and
explains *why* downstream units depend on it.

Source: `docs/API.md` (kept under version control; rendered here via
MyST include).

```{include} API.md
:start-line: 1
```
