# silica.engine

The top-level `Engine` class: the only entry point that wires
`ModelAdapter`, `ContinuousBatcher`, `RadixPrefixCache`, and
`SamplingParams` into a runnable inference loop.

```{eval-rst}
.. automodule:: silica.engine
   :members:
   :undoc-members:
   :show-inheritance:
```
