# Chat CLI

The bundled chat REPL exercises silica's signature features —
cross-turn KV reuse via `RadixPrefixCache`, optional KV codec, live
toolbar with TTFT / decode-throughput / prefix-store residency.

Design contract: `plans/CHAT_CLI_OPENING.md`.

```{include} ../CHAT.README.md
:start-line: 1
```
